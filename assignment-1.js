var numbers = []
for (var i=0; i < 10; i++){ // set numbers that will be included in array
    numbers.push(i); // pushes above numbers into the array
console.log(numbers[0]); // logs first array number
}
console.log(numbers[9]); // logs last array number
var car = { } //creates an object
car.colour = "black" ; // assigns it the color black
